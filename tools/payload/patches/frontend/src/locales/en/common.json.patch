diff --git a/frontend/src/locales/en/common.json b/frontend/src/locales/en/common.json
index 9cd8c2b..371cc49 100644
--- a/frontend/src/locales/en/common.json
+++ b/frontend/src/locales/en/common.json
@@ -28,6 +28,10 @@
     "edit": "Edit",
     "delete": "Delete",
     "add": "Add Output",
+    "add_board_output": "Board Output",
+    "add_expander_output": "Expander Output",
+    "expander_output_label": "Expander Output",
+    "expander_slot": "Expander Slot",
     "boneio_output": "boneIO Output",
     "custom_id": "Custom ID",
     "display_name": "Display Name",
@@ -82,6 +86,16 @@
     "software_interlock_desc1": "Prevents multiple outputs in the same group from being ON at the same time.",
     "software_interlock_desc2": "Useful for motor direction control, mutually exclusive devices, etc.",
     "output_type": "Output Type",
+    "divider_hardware": "Hardware routing",
+    "mcp_id": "MCP Expander",
+    "mcp_id_placeholder": "Select MCP expander...",
+    "mcp_id_board_default": "Board default (from mapping)",
+    "mcp_id_hint": "Override which MCP23017 expander drives this output. Saved as kind: mcp in YAML.",
+    "mcp_pin": "MCP Pin",
+    "mcp_pin_short": "pin",
+    "mcp_pin_placeholder": "Select pin...",
+    "mcp_pin_hint": "Pin number on the MCP23017 (0–15). Requires MCP expander selection.",
+    "mcp_expander_output_locked": "Hardware routing is fixed for expander outputs",
     "restore_state": "Restore State",
     "momentary_turn_on": "Momentary Turn On",
     "momentary_turn_off": "Momentary Turn Off",
@@ -673,6 +687,7 @@
     "remote_inputs": "Remote Inputs",
     "output": "Local Outputs",
     "remote_outputs": "Remote Outputs",
+    "remote_sensors": "Remote Sensors",
     "output_group": "Output Groups",
     "cover": "Covers",
     "remote_devices": "Remote Devices",
@@ -689,6 +704,7 @@
       "remote_inputs": "Binary sensors from remote ESPHome and CAN devices. Configure mode (binary sensor or event) and actions for each remote input.",
       "output": "Configure relay outputs to control lights, switches, and other devices.",
       "remote_outputs": "Switches and lights from remote ESPHome and CAN devices. Once registered, remote outputs can be used in irrigation zones, output groups, and templates.",
+      "remote_sensors": "Numeric/string sensors from remote MQTT devices (temperature, humidity, status fields extracted from JSON payloads). Each sensor subscribes to one MQTT topic and emits values through the same path as on-board sensors.",
       "output_group": "Group multiple outputs together to control them as a single entity.",
       "cover": "Configure covers like blinds, shutters, and garage doors with open/close relays.",
       "remote_devices": "Configure remote devices to control.",
@@ -722,10 +738,27 @@
   "mcp": {
     "info_title": "MCP23017 Configuration",
     "info_description": "Change I2C addresses of GPIO expanders. Default addresses are set automatically by board configuration. Change only if your board has different addresses.",
+    "section_onboard": "Onboard chips",
+    "section_expander": "Expansion board",
+    "chip_left": "Left chip",
+    "chip_right": "Right chip",
+    "expander_chip_left": "Expander left chip",
+    "expander_chip_right": "Expander right chip",
     "left_description": "Left GPIO expander (OUT_01-OUT_16 for 32x10, OUT_01-OUT_12 for 24x16)",
     "right_description": "Right GPIO expander (OUT_17-OUT_32 for 32x10, OUT_13-OUT_24 for 24x16)",
+    "expander_left_description": "External left expander chip",
+    "expander_right_description": "External right expander chip",
     "address": "I2C Address",
-    "address_hint": "Select I2C address for this expander"
+    "address_hint": "Select I2C address for this expander",
+    "expander_outputs_active": "outputs",
+    "expander_board_type": "Board Type",
+    "expander_outputs_preview": "Will add {count} outputs ({first} … {last})",
+    "expander_type_unknown": "Unknown type",
+    "expander_managed_in_boneio": "Add or remove the expansion board from the BoneIO section.",
+    "expander_add": "Add expansion board",
+    "expander_remove": "Remove expansion board",
+    "expander_remove_confirm": "This will remove the expansion board and all {count} its outputs. Continue?",
+    "expander_remove_blocked": "Cannot remove — inputs have actions linked to expander outputs. Remove them first:"
   },
   "oled": {
     "enabled": "OLED Display Enabled",
@@ -913,7 +946,13 @@
     "cloud_permission_error": "docker-compose.yaml is not writable. Run via SSH: sudo chown $USER ~/docker/nodered/docker-compose.yaml",
     "cloud_permission_fix_hint": "Enter your system password to fix file permissions automatically:",
     "cloud_sudo_placeholder": "System password",
-    "cloud_fix_btn": "Fix"
+    "cloud_fix_btn": "Fix",
+    "expander_section": "Expansion Board",
+    "expander_add": "Add Expansion Board",
+    "expander_active": "Configured",
+    "expander_remove": "Remove Expansion Board",
+    "expander_remove_confirm": "Remove expansion board and all its outputs?",
+    "expander_saved": "Expansion board saved. Restart required."
   },
   "mqtt_config": {
     "host": "Host",
@@ -1532,6 +1571,43 @@
     "enforce_interlock_hint": "If turned ON externally (e.g. from ESPHome/HA) and it violates interlock, boneIO will immediately turn it OFF.",
     "interlock_shared_desc": "Remote outputs share interlock groups with local outputs. When an output in a group is ON, other outputs in the same group cannot be turned ON via boneIO."
   },
+  "remote_sensors": {
+    "name_placeholder": "Friendly name (defaults to sensor id)",
+    "custom_id": "Custom entity ID",
+    "custom_id_placeholder": "auto: <device>_<sensor>",
+    "custom_id_hint": "Leave empty to auto-generate from device + sensor IDs.",
+    "sensor_entity": "Sensor",
+    "select_device": "Select device…",
+    "select_sensor": "Select sensor…",
+    "no_devices_hint": "No generic MQTT devices with sensors. Add sensors to a device first (Remote Devices form).",
+    "from_device": "From device",
+    "from_device_hint": "Topic and template live on the device. Edit them in the Remote Devices form.",
+    "show_in_ha": "Forward to Home Assistant",
+    "show_in_ha_hint": "Publish HA autodiscovery so the sensor appears in Home Assistant.",
+    "unit": "Unit",
+    "unit_hint": "Overrides the unit declared on the device. Leave empty to use the device value.",
+    "device_class": "device_class",
+    "select_device_class": "Select device_class…",
+    "state_class": "state_class"
+  },
+  "remote_mqtt": {
+    "browse_broker": "Browse MQTT broker…",
+    "browse_broker_hint": "Discover devices and topics live on your MQTT broker",
+    "browse_broker_title": "MQTT broker — devices and topics",
+    "browse_broker_description": "Click a branch to set it as this device's topic prefix, or use the per-topic + buttons to add a single topic as input / output / sensor.",
+    "tree_results_summary": "{n} topics on `{pattern}`",
+    "rescan": "Re-scan",
+    "scanning_status": "Listening on `{pattern}` for {duration}s …",
+    "field_topic_prefix": "Topic prefix",
+    "topic_prefix_hint": "Common prefix of all topics for this device (e.g. n64/88). Per-entity topics are stored individually below.",
+    "section_inputs": "Inputs",
+    "section_outputs": "Outputs",
+    "section_sensors": "Sensors",
+    "add_input": "Add input",
+    "add_output": "Add output",
+    "add_sensor": "Add sensor",
+    "duplicate_id": "Duplicate ID — entity IDs must be unique on a device"
+  },
   "table_sort": {
     "click_to_sort": "Click to sort",
     "sorted_asc": "Sorted ascending",
@@ -1927,6 +2003,61 @@
     "apply_started": "Migration apply started. Refresh in a moment.",
     "apply_failed": "Apply failed: {{error}}"
   },
+  "remote_mqtt": {
+    "scan_button": "Scan broker",
+    "scan_button_tooltip": "Discover what topics the MQTT broker is publishing",
+    "scan_dialog_title": "Scan MQTT broker",
+    "scan_dialog_description": "Subscribe to a topic pattern for a few seconds to see what the broker is currently publishing. Use this to discover devices that don't follow boneIO's topic convention (e.g. ROPAM alarm panels).",
+    "pattern_label": "Topic pattern",
+    "duration_label": "Duration (s)",
+    "scanning": "Scanning…",
+    "scanning_status": "Listening on `{pattern}` for {duration}s …",
+    "pattern_invalid": "Invalid MQTT topic pattern. Use `/` separators, `+` for single-level wildcards, `#` for multi-level (only as last segment).",
+    "filter_placeholder": "Filter topics or payloads…",
+    "col_topic": "Topic",
+    "col_type": "Type",
+    "col_payload": "Last payload",
+    "col_updates": "Upd.",
+    "results_truncated": "+{n} more topics — refine the filter to see them.",
+    "no_results": "No messages received on `{pattern}` in {duration}s. Try a broader pattern or run again — some devices publish on retained topics only on connect.",
+    "clear_results": "Clear",
+    "payload_label": "Payload",
+    "template_label": "value_template",
+    "template_hint": "Click any value above to fill in the path",
+    "preview_label": "Preview",
+    "preview_pending": "Evaluating…",
+    "preview_empty_template": "Enter a template to see the preview",
+    "field_topic": "MQTT topic",
+    "field_command_topic": "Command topic",
+    "field_command_template": "command_template",
+    "field_state_topic": "State topic (feedback)",
+    "field_state_value_template": "state_value_template",
+    "field_payload_on": "payload_on (optional)",
+    "field_payload_off": "payload_off (optional)",
+    "topic_invalid": "Invalid topic (no spaces, no `+` or `#` wildcards in publication topics).",
+    "template_hint_input": "Defaults to `{{ value }}` (raw payload). For JSON: `{{ value_json.fail }}`.",
+    "command_template_hint": "Context: `{{ state }}` (\"ON\"/\"OFF\"), `{{ brightness }}` (0-255). Default sends \"ON\"/\"OFF\" string.",
+    "test_payload_label": "Test against a sample payload",
+    "test_payload_hint": "Paste a sample payload from your device to verify the template.",
+    "state_topic_hint": "If set, boneIO subscribes here and updates state from the real device. Otherwise uses optimistic local state after publish.",
+    "state_feedback_label": "State feedback (optional)",
+    "inputs_from_device_hint": "Inputs are managed on the device. Add / edit them in Remote Devices → MQTT settings.",
+    "outputs_from_device_hint": "Outputs are managed on the device. Add / edit them in Remote Devices → MQTT settings.",
+    "field_topic_prefix": "Topic prefix (optional)",
+    "topic_prefix_hint": "Used to scope the Scan & import dialog. Per-entity topics still stored individually below.",
+    "topic_prefix_required": "Set a topic prefix first",
+    "scan_and_import": "Scan & import",
+    "section_inputs": "Inputs",
+    "section_outputs": "Outputs",
+    "add_input": "Add input",
+    "add_output": "Add output",
+    "scan_import_title": "Scan broker & import topics",
+    "scan_import_description": "Select topics to add as inputs on this device. Templates auto-set from payload type.",
+    "select_all": "Select all",
+    "select_none": "None",
+    "import_selected": "Import {n} as inputs",
+    "duplicate_id": "Duplicate ID — entity IDs must be unique on a device"
+  },
   "actions": {
     "delay_execution": "Delay execution",
     "delay_execution_hint": "Wait before executing this action. Timer resets if cancel event occurs.",
@@ -1960,4 +2091,4 @@
     "quick_setup": "Quick action setup",
     "quick_setup_hint": "Configure actions for motion sensor, door sensor, doorbell — one click"
   }
-}
\ No newline at end of file
+}
